/// The basic utf-8 string type.
/// Only provides basic grapheme iteration (not grapheme clusters) and other simple string operations.
mod type enum {
    char: char,
    str: str,
};

let char = type 0..255;

let str = type []char;
