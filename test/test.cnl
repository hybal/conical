type Pos = struct {
   x: i32;
   y: i32;
};

type Option = enum {
    Some: i32,
    None
};

type Example = i32;
fn main() -> Example {
    let a: Example = 1;
    a
}

fn wee(a): i32 -> i32 {
    let b: i32 = 1;
    b
}

