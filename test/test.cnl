type Pos = struct {
   x: i32;
   y: i32;
};

type Option = enum {
    Some: i32,
    None
};

type Example = i32;
fn main() -> i32 {
    0
}

fn init_pos(x, y): (i32, i32) -> Pos {
    Pos {
        .x = x,
        .y = y
    }
}

