mod main;
extern fn print();
extern fn puts(value): &u8 -> i32;


fn add(a, b): (i32, i32) -> i32 {
    return a + b;
}
pub type Test = struct {
    a: i32,
    b: i32
};

pub fn main() -> i32 {
    let a: i32 = 1;
    let b: Test = Test { .a = 1, .b = 2 };
    let out = add(1, 3);
    if b.a == 1 {
        puts("Hi");
    } else {
        puts("bye");
    }
    return out;
}


