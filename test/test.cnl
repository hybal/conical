
extern fn print();
extern fn puts(value): (&rune) -> i32;

type A = i32;
fn main() -> A {
    let a = 4;
    let v = "Hello, World!\0";
    //print();
    add(1, a)
}

fn add(a, b): (i32, i32) -> i32 {
    a + b
}
