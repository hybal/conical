const std = @import("std");
const testing = std.testing;
const expect = testing.expect;
const set = @import("set.zig");
const ty = @import("type.zig");

test "regression/subset" {
//    const allocator = std.heap.page_allocator;
//    var a = set.Set.init(allocator);
//    var b = set.Set.init(allocator);
//    try a.putIRange(.{.start = 5, .end = 7 });
//    try b.putIRange(.{.start = 3, .end = 10});
//    try expect(a.compare(&b) == .subset);

}
